{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "npx lerna run dev --stream",
    "gen": "npx lerna run gen --stream --scope dynanotes-ui",
    "build": "bash ./scripts/build.sh"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
